# sidebearing-trim

This repository contains:

- `packages/sidebearing-trim`: publishable npm package (core, vanilla and React exports)
- `examples/`: vanilla and React usage examples
- `docs/`: custom Vite + React GitHub Pages site

## Local development

```bash
npm install
npm run build
npm run test
```

Run docs locally:

```bash
npm run dev:docs
```
